/**
 * Visible build fingerprint.
 *
 * Shown on the boot spinner and login footer. Bump it each push so a
 * sideloaded build can be identified from the on-screen text alone — that
 * lets us tell a stale IPA apart from a real runtime hang on a device that
 * has no console.
 */
export const BUILD_TAG = 'b29';